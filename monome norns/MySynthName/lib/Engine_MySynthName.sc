Engine_MySynthName : CroneEngine {

	// ** add variables here **

	// Initializes SynthDefs, can call sync and wait methods after this is done
	alloc { // allocate memory to the following

		// ** add SynthDefs here **

		// Uncomment if you need SynthDefs to be available before commands are sent
		// Server.default.sync

		// ** add commands here **
	}
}